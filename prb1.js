//El programa recibe dos instrucciones y un mensaje, y el resultado debe ser si existe o no una instrucción escondida en el mensaje.
// La entrada al programa es un archivo de texto que consiste en cuatro líneas.
function uncode(txtfile) {
  // o La primera línea son tres enteros M1, M2 y N.
  let line1 = {
    //M1 y M2 es el número de caracteres de las dos instrucciones
    //▪ M1 y M2 siempre estarán entre 2 y 50 inclusive
    m1: Number,
    m1: Number,
    //y N es el número de caracteres en el mensaje.
    // N siempre estará entre 3 y 5000 inclusive
    n: Number,
  };
  //o Lasegundalíneacontienelaprimerainstrucción
  let line2 = "1st intruccion";
  //   o Laterceralíneacontienelasegundainstrucción
  let line3 = "2 intruccion";
  //La cuarta línea contiene el mensaje,  Los caracteres posibles en el mensaje son [a-zA-Z0-9]
}
